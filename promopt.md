# 🧠 AI Coding Prompt：CargoNight 在线桌游平台开发



## 一、项目目标

请使用figma MCP 根据 https://www.figma.com/design/EqeiPk1hiECVSdfxEKwPCm/%E6%A1%8C%E6%B8%B8%E8%AE%BE%E8%AE%A1?node-id=0-1&p=f&t=lx0RwfC3TCdChRyd-0 设计前端页面，要求显示内容与figma设计的页面像素级还原并且自动适配多种平台布局。技术栈为：

* 后端/核心逻辑：Rust
* 桌面端框架：Tauri 2
* 前端：React + Vite + Tailwind
* 目标平台：Windows / Linux / Android / iOS

最终产物必须是一个：

* 页面结构完整、可交互
* 前后端联动清晰
* 可跨平台打包
* 游戏逻辑完整
* 真正的桌游应用

---

## 二、前端页面结构

前端设计包含以下核心页面，必须严格按照figma页面设计：

1. 首页（HomePage）
   * 平台概览：在线玩家、活跃房间、游戏中数据
   * 热门游戏展示
   * 热门房间列表
   * 在线好友与公告侧边栏
   * 快速进入/浏览游戏按钮

2. 游戏选择页（GameSelectionPage）
   * 游戏卡片列表
   * 每个游戏显示：名称、描述、玩家数、时长、难度、热门标签
   * 进入游戏大厅按钮

3. 游戏大厅页（GameLobbyPage）
   * 房间搜索与筛选
   * 创建房间弹窗
   * 房间列表：房间名称、房主、人数、私密状态、游戏模式、状态、加入/观战按钮
   * 当前游戏类型（如阿瓦隆、狼人杀等）

4. 房间页（GameRoomPage）
   * 房间信息头部：房名、房主、游戏、状态
   * 玩家列表：准备状态、房主标记、空位提示
   * 聊天区：消息列表、输入框
   * 操作按钮：邀请好友、准备、开始游戏、离开房间

5. 个人资料页（ProfilePage）

---

## 三、优化目标

请将当前需求文档优化为能够指导开发者：

* 正确搭建项目结构
* 按页面设计实现各个功能模块
* 使用 Rust + Tauri 实现后端逻辑与前端交互
* 支持窗口端与移动端适配

重点关注：

* 现有页面路由与组件层次
* 页面之间的状态传递与导航流程
* 房间创建/加入/聊天/准备/开始逻辑
* 前端 UI 与后端命令的契合

---

## 四、功能需求

### 1. 页面与界面

#### 首页

* 显示平台总体数据
* 展示热门游戏卡片
* 列出热门房间与在线好友
* 提供快速入口：快速开始、浏览游戏

#### 游戏选择页

* 展示可选桌游列表
* 每个游戏卡片显示关键信息
* 支持进入对应游戏大厅

#### 游戏大厅页

* 支持房间搜索与筛选
* 支持创建房间
* 房间列表展示加入/观战入口
* 显示当前游戏类型和在线人数

#### 房间页

* 显示房间基本信息（名称、房主、状态）
* 玩家列表展示准备状态与空位
* 房主可开始游戏
* 支持聊天输入与展示
* 支持邀请/离开操作
* 游戏人数和游戏类型设置
* 房间显示/隐藏/加密等

### 2. 通信与数据流

* 前端通过 Tauri `invoke` 调用 Rust 命令
* Rust 管理游戏状态、房间列表、玩家状态、聊天消息
* 前端展示来自 Rust 的状态数据，并支持主动更新
* 设计清晰 API：
  * `get_home_stats`
  * `get_game_list`
  * `get_lobby_rooms(gameType)`
  * `create_room(roomData)`
  * `join_room(roomId)`
  * `leave_room(roomId)`
  * `toggle_ready(roomId, playerId)`
  * `send_chat(roomId, message)`
  * `start_game(roomId)`

### 3. 状态管理

* 使用组件化状态方案，避免页面重复渲染
* 推荐采用简单全局状态或 context，或可选 Redux/Zustand
* 支持房间列表过滤与搜索
* 房间页支持实时聊天更新与玩家状态变化

### 4. 跨平台适配

* 保留现有 `tailwind` 响应式布局
* 确保移动端触控优先，UI 按钮、列表、滚动可用
* 桌面端适配窗口大小变化
* Tauri 构建支持 Android/iOS（可用 `tauri-mobile` 或 hybrid 方案）

### 5. 性能与工程化

* Rust 侧确保核心逻辑高效、状态变更最小化
* 前端侧避免大规模重渲染
* 提供结构化代码与清晰目录
* 给出构建、运行、打包说明

---

## 五、输出要求

请按以下结构输出方案与内容：

1. 项目整体架构说明
2. 目录结构树
3. 核心代码示例（Rust + 前端关键页面/命令）
4. 前后端通信示例
5. 构建与运行方式
6. 多端打包方案

---

## 六、代码规范

* Rust：符合 `idiomatic Rust`，类型与错误处理清晰
* 前端：React 组件化、命名规范、Tailwind 语义清晰
* 注释中英文可读

---

## 七、扩展与加分

可在方案中额外说明：

* 使用状态管理库（如 Redux / Zustand / Context）
* 使用动画 / 过渡增强用户体验
* 设计联网对战扩展接口
* 支持前端热更新/快速开发模式

---

## 八、注意事项

* 不要只写伪代码，输出应具有可实施性
* 重点是“前端页面设计 + Rust/Tauri 跨平台实现”，需要实现一个完整的线上桌游平台
