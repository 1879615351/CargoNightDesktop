// 服务器连接配置 - 修改此文件即可统一修改所有网络地址
export const SERVER_IP = "172.29.11.177";
export const SERVER_PORT = 8080;

export const API_BASE = `http://${SERVER_IP}:${SERVER_PORT}/api`;
export const WS_BASE = `ws://${SERVER_IP}:${SERVER_PORT}/ws`;
